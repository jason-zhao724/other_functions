javascript:(asyncfunction(){constcdnBaseList=["https://dla.3yjt.com","https://dlcd.3yjt.com","https://dlray.3yjt.com","https://dljb.3yjt.com","https://game.3yjt.com","https://i1tq4bgq3xg.3yjt.com","https://dlcd.skytvro.com","https://dlaws.ecosysmail.com","https://dlg.ecosysmail.com","https://k25ryj8x.ecosysmail.com"];consttimeoutMs=8000;constconcurrency=12;constslowThreshold=800;constmaxUrls=5000;constcdnResUrl=(window.gameSetting&&window.gameSetting.cdnResUrl)||null;if(!cdnResUrl){console.error("❌找不到gameSetting.cdnResUrl。請確認您是否在遊戲Frame內執行，或gameSetting是否已載入。");alert("❌找不到gameSetting.cdnResUrl\n請按F12查看Console");return}constcdnResPrefix=cdnResUrl.endsWith("/")?cdnResUrl:(cdnResUrl+"/");const resBasePath=new URL(cdnResPrefix).pathname;performance.setResourceTimingBufferSize(10000);const entries=performance.getEntriesByType("resource");const matched=[];const seen=new Set;for(const e of entries){const u=e.name;if(!u||!u.startsWith(cdnResPrefix))continue;if(seen.has(u))continue;seen.add(u);matched.push(u);if(matched.length>=maxUrls)break}const relPaths=matched.map(u=>u.slice(cdnResPrefix.length));console.clear();console.group("📦 本次載入的 cdnResUrl 資源清單");console.log("cdnResUrl =",cdnResPrefix);console.log("總數 =",relPaths.length);console.groupEnd();if(relPaths.length===0){console.warn("沒有抓到任何 cdnResUrl 下的資源。");alert("⚠️ 沒有抓到相關資源，請先讓遊戲跑一段時間後再點擊測試。");return}console.log("🚀 測試開始，請稍候...");async function testUrl(url){const start=performance.now();const controller=new AbortController;const timer=setTimeout(()=>controller.abort(),timeoutMs);const done=(ok,extra={})=>{const time=Math.round(performance.now()-start);clearTimeout(timer);return{ok:ok,time:time,...extra,result:ok?(time>slowThreshold?"⚠️ SLOW":"✅ OK"):"❌ ERROR"}};try{const r=await fetch(url,{method:"HEAD",cache:"no-store",mode:"cors",signal:controller.signal});return done(r.ok,{status:r.status,mode:"HEAD(cors)"})}catch(e1){try{const r=await fetch(url,{method:"GET",cache:"no-store",mode:"cors",signal:controller.signal});return done(r.ok,{status:r.status,mode:"GET(cors)"})}catch(e2){if(/\.(png|jpg|jpeg|webp|gif|bmp)(\?|$)/i.test(url)){try{const imgOk=await new Promise(resolve=>{const img=new Image;const t=setTimeout(()=>resolve(false),timeoutMs);img.onload=()=>{clearTimeout(t);resolve(true)};img.onerror=()=>{clearTimeout(t);resolve(false)};img.src=url+(url.includes("?")?"&":"?")+"cdnProbe="+Date.now()});return done(imgOk,{status:imgOk?"LOAD":"ERROR",mode:"IMG"})}catch(e3){return done(false,{status:"FAILED",mode:"IMG",error:String(e3)})}}return done(false,{status:"FAILED",mode:"FETCH",error:String(e2)})}}}function makeTestUrl(base,rel){return base.replace(/\/+$/,"")+resBasePath+rel}const jobs=[];for(const base of cdnBaseList){for(const rel of relPaths){jobs.push({base:base,rel:rel,url:makeTestUrl(base,rel)})}}console.log(`🧪 開始測試：CDN=${cdnBaseList.length}，資源=${relPaths.length}，總請求=${jobs.length}`);const results=[];let idx=0;async function worker(){while(idx<jobs.length){const my=jobs[idx++];const r=await testUrl(my.url);results.push({CDN:my.base,RelPath:my.rel,Status:r.status,Time_ms:r.time,Mode:r.mode,Result:r.result,URL:my.url})}}await Promise.all(Array.from({length:concurrency},()=>worker()));const byCdn={};for(const r of results){const k=r.CDN;(byCdn[k]||={total:0,ok:0,slow:0,fail:0}).total++;if(r.Result==="✅ OK")byCdn[k].ok++;else if(r.Result==="⚠️ SLOW")byCdn[k].slow++;else byCdn[k].fail++}const summary=Object.entries(byCdn).map(([cdn,s])=>({CDN:cdn,Total:s.total,OK:s.ok,SLOW:s.slow,FAIL:s.fail,SuccessRate:((s.ok+s.slow)/s.total*100).toFixed(1)+"%"})).sort((a,b)=>(b.OK+b.SLOW)-(a.OK+a.SLOW));const fails=results.filter(r=>r.Result.startsWith("❌"));console.group("📡 CDN 批次驗測結果");console.table(summary);console.log("❌ FAIL 明細：");console.table(fails.slice(0,200));console.log("results（完整明細）=",results);console.groupEnd();window.__cdnProbe={cdnResUrl:cdnResPrefix,relPaths:relPaths,results:results,summary:summary,fails:fails};console.log("✅ 已保存到 window.__cdnProbe");alert(`✅ 測試完成！\n\n最佳 CDN: ${summary[0]?summary[0].CDN:"無"}\n成功率: ${summary[0]?summary[0].SuccessRate:"0%"}\n\n詳細報表請按 F12 查看 Console`);})();
