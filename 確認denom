javascript:(function(){letprobData=puremvc.Facade.getInstance().model.proxyMap.GameDataProxy.probData;if(probData.denomination==undefined){alert("取不到denomination請確認遊戲是否含有denomination");return;}letbetRange=probData.betRange;for(letindexinbetRange){letvalue=betRange[index]/100/probData.denomination;if(!Number.isInteger(value)||value<=0){console.warn(`⚠️betRange[${index}]=${betRange[index]/100}除以denomination(${probData.denomination})=${value}→不是大於0的整數`);}else{console.log(`✅betRange[${index}]=${betRange[index]/100}/${probData.denomination}=${value}(OK)`);}}})();
